{
  "name": "@packages/constants",
  "version": "0.1.0",
  "private": true,
  "description": "Shared constants for Prediction Copilot (addresses, configs, limits)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
